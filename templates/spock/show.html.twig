{% extends 'base.html.twig' %}

{% block title %}Spock{% endblock %}

{% block body %}
    <h1>Spock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spock.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ spock.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spock_index') }}">back to list</a>

    <a href="{{ path('spock_edit', {'id': spock.id}) }}">edit</a>

    {{ include('spock/_delete_form.html.twig') }}
{% endblock %}
